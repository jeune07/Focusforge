export const todoReducer = (inicialState, action) => {
  switch (action.type) {
    case value:
      throw new Error("action.type =  ABC no esta implementada");

      break;

    default:
      break;
  }
};
